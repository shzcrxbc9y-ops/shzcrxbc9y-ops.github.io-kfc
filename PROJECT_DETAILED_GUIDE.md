# 📚 Подробное руководство по проекту KFC Training Platform

## 🎯 Обзор проекта

**KFC Training Platform** — это веб-приложение для обучения и сертификации сотрудников KFC. Платформа позволяет автоматически импортировать обучающие материалы из различных форматов файлов (PDF, DOCX, PPTX, XLSX), организовывать их по станциям и разделам, а также проводить тестирование и выдавать сертификаты.

---

## 🏗️ Архитектура проекта

### Технологический стек

1. **Frontend:**
   - **Next.js 14** (App Router) — React-фреймворк для серверного рендеринга
   - **TypeScript** — типизированный JavaScript
   - **Tailwind CSS** — утилитарный CSS-фреймворк
   - **React Hook Form** — управление формами
   - **Zod** — валидация данных

2. **Backend:**
   - **Next.js API Routes** — серверные эндпоинты
   - **Prisma ORM** — работа с базой данных
   - **SQLite** — база данных (файл `dev.db`)
   - **JWT** — аутентификация пользователей
   - **bcryptjs** — хеширование паролей

3. **Обработка файлов:**
   - **pdf-parse** — извлечение текста и изображений из PDF
   - **mammoth** — конвертация DOCX в HTML/текст
   - **xlsx** — чтение Excel файлов
   - **jszip** — работа с ZIP-архивами (PPTX)
   - **pptxgenjs** — генерация PowerPoint презентаций

---

## 📁 Структура проекта

```
kfc.1/
├── app/                          # Next.js App Router
│   ├── api/                      # API endpoints
│   │   ├── auth/                 # Аутентификация
│   │   │   ├── login/route.ts
│   │   │   ├── logout/route.ts
│   │   │   ├── register/route.ts
│   │   │   └── me/route.ts
│   │   └── tests/                 # API для тестов
│   ├── admin/                    # Админ-панель
│   │   ├── materials/page.tsx
│   │   ├── tests/page.tsx
│   │   ├── users/page.tsx
│   │   └── statistics/page.tsx
│   ├── learning/                  # Обучающие материалы
│   │   ├── page.tsx
│   │   └── material/[id]/page.tsx
│   ├── tests/                     # Тестирование
│   │   ├── page.tsx
│   │   └── [id]/page.tsx
│   ├── dashboard/                 # Личный кабинет
│   ├── certification/             # Сертификация
│   ├── login/                     # Страница входа
│   ├── register/                  # Регистрация
│   └── page.tsx                    # Главная страница
│
├── components/                    # React компоненты
│   ├── AuthProvider.tsx           # Провайдер аутентификации
│   └── Navbar.tsx                 # Навигационная панель
│
├── lib/                           # Утилиты
│   ├── auth.ts                    # Функции аутентификации
│   ├── prisma.ts                  # Prisma клиент
│   └── utils.ts                   # Вспомогательные функции
│
├── prisma/                        # База данных
│   ├── schema.prisma              # Схема БД
│   ├── dev.db                     # SQLite база данных
│   └── seed.ts                    # Начальные данные
│
├── scripts/                       # Скрипты обработки файлов
│   ├── extract-info.ts            # Извлечение контента из файлов
│   ├── integrate-info.ts          # Интеграция в БД
│   ├── cleanup-materials.ts       # Очистка дубликатов
│   ├── organize-sections.ts      # Организация разделов
│   └── ...                        # Другие утилиты
│
├── информация/                    # Исходные файлы для импорта
│   ├── *.pdf
│   ├── *.docx
│   ├── *.pptx
│   └── *.xlsx
│
├── public/                        # Статические файлы
│   ├── images/                    # Изображения из PDF
│   ├── pdfs/                      # PDF файлы для просмотра
│   └── presentations/             # PPT файлы
│
└── package.json                   # Зависимости и скрипты
```

---

## 🗄️ База данных (Prisma Schema)

### Основные модели:

1. **User** — Пользователи системы
   - Роли: ADMIN, MENTOR, EMPLOYEE
   - Связи: Progress, TestResult, Certification, Achievements

2. **Station** — Станции работы (Касса, Кухня, Панировка)
   - Связи: Sections, Certifications

3. **Section** — Разделы внутри станций
   - Примеры: "Чек-листы", "Рутины", "Операционное руководство"
   - Связи: Materials, Tests

4. **Material** — Обучающие материалы
   - Типы: text, video, image
   - Содержит HTML контент

5. **Test** — Тесты
   - Проходной балл, лимит времени
   - Связи: Questions, TestResults

6. **Question** — Вопросы теста
   - Типы: single (один ответ), multiple (несколько)
   - Связи: Options

7. **TestResult** — Результаты прохождения тестов
   - Статусы: NOT_STARTED, IN_PROGRESS, COMPLETED, PASSED, FAILED
   - Связи: Answers

8. **Certification** — Сертификация по станциям
   - Статусы: NOT_STARTED, IN_PROGRESS, PASSED, FAILED

9. **Progress** — Прогресс обучения пользователя
   - Счетчики: sectionsCompleted, testsCompleted, certificationsPassed
   - Уровень и опыт

10. **Achievement** — Достижения
    - Условия получения в формате JSON

---

## 🔄 Процесс работы системы

### Шаг 1: Подготовка файлов

Поместите обучающие материалы в папку `информация/`:
- PDF файлы (руководства, чек-листы)
- DOCX файлы (справочники, инструкции)
- PPTX файлы (презентации, тренинги)
- XLSX файлы (таблицы, рутины)

### Шаг 2: Извлечение контента

Запустите скрипт извлечения:
```bash
npm run extract-info
```

**Что происходит:**
1. Скрипт `scripts/extract-info.ts` читает все файлы из папки `информация/`
2. Для каждого файла определяется тип (PDF, DOCX, PPTX, XLSX)
3. Извлекается содержимое:
   - **PDF**: текст и изображения (если текст минимальный)
   - **DOCX**: текст через библиотеку `mammoth`
   - **PPTX**: текст из слайдов через `jszip` (парсинг XML)
   - **XLSX**: данные из всех листов через `xlsx`
4. Результаты сохраняются в `extracted-info.json`

**Особенности обработки PDF:**
- Если текст < 100 символов, система пытается извлечь изображения
- Изображения сохраняются в `public/images/`
- Если изображения найдены, возвращается маркер `[PDF_WITH_IMAGES]`

**Особенности обработки PPTX:**
- Файл распаковывается как ZIP
- Читаются XML файлы слайдов из `ppt/slides/slide*.xml`
- Текст извлекается удалением XML тегов
- Формируется структура: `=== Слайд N ===\nтекст слайда`

### Шаг 3: Интеграция в базу данных

Запустите скрипт интеграции:
```bash
npm run integrate-info
```

**Что происходит:**
1. Скрипт `scripts/integrate-info.ts` читает `extracted-info.json`
2. Для каждого файла определяется категория:
   - **Функция `categorizeFile()`** анализирует имя файла
   - По ключевым словам определяется станция и раздел
   - Примеры:
     - "кликун" → Станция кассы
     - "кухн" → Станция кухни
     - "паниров" → Станция кухни / Панировка
     - "l3" → Общие стандарты / Обучение и тренинги

3. Создаются/обновляются записи:
   - **Station** (если не существует)
   - **Section** (если не существует)
   - **Material** с извлеченным контентом

4. Обработка контента:
   - **PDF с изображениями**: изображения сохраняются, создается HTML с `<img>` тегами
   - **PDF с текстом**: текст разбивается на части (если > 10000 символов), конвертируется в HTML
   - **PPTX**: создается красиво отформатированный HTML с разбивкой по слайдам
   - **DOCX/XLSX**: текст конвертируется в HTML параграфы

5. Для PDF файлов, которые нельзя извлечь:
   - Файл копируется в `public/pdfs/`
   - Создается материал с iframe для просмотра

### Шаг 4: Организация материалов

После интеграции можно запустить скрипты организации:

```bash
# Удаление дубликатов
npm run cleanup-materials

# Организация разделов
npm run organize-sections

# Объединение станций
npm run merge-stations

# Финальная организация
npm run final-organize
```

---

## 🎨 Интерфейс пользователя

### Главная страница (`/`)
- Приветствие и описание платформы
- Карточки с ссылками на основные разделы
- Кнопка "Начать обучение"

### Обучающие материалы (`/learning`)
- Список станций (Касса, Кухня, Общие стандарты)
- Для каждой станции — список разделов
- В каждом разделе — материалы
- При клике на материал открывается страница с контентом

### Просмотр материала (`/learning/material/[id]`)
- Отображается HTML контент материала
- Для PDF с изображениями — галерея изображений по страницам
- Для PPTX — красиво отформатированные слайды
- Для обычного текста — структурированный HTML

### Тестирование (`/tests`)
- Список доступных тестов
- При клике — страница прохождения теста
- После завершения — страница с результатами

### Личный кабинет (`/dashboard`)
- Прогресс обучения
- Статистика прохождения тестов
- Достижения
- Сертификаты

### Админ-панель (`/admin`)
- Управление материалами
- Управление тестами
- Управление пользователями
- Статистика по платформе

---

## 🔐 Аутентификация

### Процесс входа:
1. Пользователь вводит email и пароль на `/login`
2. API `/api/auth/login` проверяет учетные данные
3. Пароль сравнивается с хешем в БД (bcrypt)
4. При успехе создается JWT токен
5. Токен сохраняется в cookies
6. Пользователь перенаправляется на `/dashboard`

### Роли пользователей:
- **ADMIN**: полный доступ ко всем функциям
- **MENTOR**: может создавать материалы и тесты
- **EMPLOYEE**: может только изучать материалы и проходить тесты

---

## 📊 API Endpoints

### Аутентификация:
- `POST /api/auth/register` — регистрация нового пользователя
- `POST /api/auth/login` — вход в систему
- `POST /api/auth/logout` — выход
- `GET /api/auth/me` — получение информации о текущем пользователе

### Тесты:
- `GET /api/tests/[id]` — получение теста
- `POST /api/tests/[id]/submit` — отправка ответов на тест

---

## 🛠️ Скрипты обработки файлов

### Основные скрипты:

1. **`extract-info.ts`**
   - Извлекает контент из всех файлов в `информация/`
   - Сохраняет результаты в `extracted-info.json`

2. **`integrate-info.ts`**
   - Читает `extracted-info.json`
   - Создает станции, разделы и материалы в БД
   - Обрабатывает PDF изображения
   - Форматирует PPTX слайды

3. **`cleanup-materials.ts`**
   - Находит и удаляет дубликаты материалов
   - Сравнивает нормализованные названия

4. **`organize-sections.ts`**
   - Объединяет дубликаты разделов
   - Упорядочивает разделы по порядку

5. **`merge-stations.ts`**
   - Объединяет похожие станции
   - Переносит материалы и разделы

6. **`remove-pdf-text-materials.ts`**
   - Удаляет материалы, созданные из текста PDF
   - Оставляет только PDF файлы для просмотра

7. **`remove-all-training-materials.ts`**
   - Удаляет все материалы из раздела "Обучение и тренинги"

---

## 🚀 Запуск проекта

### 1. Установка зависимостей:
```bash
npm install
```

### 2. Настройка базы данных:
```bash
# Создать Prisma клиент
npm run db:generate

# Применить схему к БД
npm run db:push

# Заполнить тестовыми данными
npm run db:seed
```

### 3. Создание файла `.env`:
```env
DATABASE_URL="file:./prisma/dev.db"
JWT_SECRET="your-secret-key-change-this"
NEXTAUTH_URL="http://localhost:3000"
```

### 4. Импорт материалов:
```bash
# Извлечь контент из файлов
npm run extract-info

# Интегрировать в БД
npm run integrate-info
```

### 5. Запуск сервера:
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

---

## 📝 Логика категоризации файлов

Функция `categorizeFile()` в `integrate-info.ts` определяет станцию и раздел по имени файла:

### Станция "Станция кассы":
- Ключевые слова: "кликун", "click", "касса", "касс"
- Разделы:
  - "Чек-листы" — если содержит "чек"
  - "Рутины" — если содержит "рутин"
  - "Закрытие и передача смены" — если содержит "закрытие", "передача", "смен"
  - "Операционное руководство" — если содержит "операционн", "руководство"
  - "Функционал системы" — если содержит "функционал"
  - "Основы работы" — по умолчанию

### Станция "Станция кухни":
- Ключевые слова: "кухн", "kitchen", "бш", "fd", "паниров", "панера"
- Разделы:
  - "Руководства" — если содержит "руководство", "бш"
  - "Параметры приготовления" — если содержит "параметр", "приготовлен"
  - "Устранение проблем" — если содержит "устранен", "проблем", "куриц"
  - "Чек-листы панировки" — если содержит "паниров" + "чек"
  - "Панировка" — если содержит "паниров"

### Станция "Общие стандарты":
- Разделы:
  - "Обучение и тренинги" — если содержит "l3", "тренинг", "тренер"
  - "Чек-листы" — если содержит "easy", "check"
  - "Карточки" — если содержит "карточк", "abr"
  - "Зарплата и рестораны" — если содержит "зп", "ресторан"
  - "Стандарты работы" — если содержит "общие", "стандарт"

---

## 🎯 Особенности обработки файлов

### PDF файлы:
1. **С текстом**: текст извлекается, разбивается на части, конвертируется в HTML
2. **С изображениями**: изображения сохраняются в `public/images/`, создается HTML с тегами `<img>`
3. **Только файл**: если извлечение не удалось, файл копируется в `public/pdfs/` для просмотра через iframe

### PPTX файлы:
- Текст извлекается из XML слайдов
- Создается HTML с красивым форматированием:
  - Каждый слайд в отдельном блоке
  - Заголовки слайдов выделены
  - Структурированный текст с параграфами
- Отображается прямо на экране (не ссылка на скачивание)

### DOCX файлы:
- Текст извлекается через `mammoth`
- Конвертируется в HTML параграфы
- Сохраняется форматирование

### XLSX файлы:
- Данные извлекаются из всех листов
- Форматируется как таблица в тексте
- Конвертируется в HTML

---

## 🔧 Утилиты и вспомогательные скрипты

### Просмотр структуры:
```bash
npm run show-structure
```
Показывает иерархию станций, разделов и материалов

### Проверка файлов:
```bash
npm run check-all-files
```
Проверяет, все ли файлы из `информация/` добавлены в БД

### Управление базой данных:
```bash
npm run db:studio
```
Открывает Prisma Studio для визуального просмотра и редактирования БД

---

## 📦 Зависимости проекта

### Основные:
- `next` — фреймворк
- `react`, `react-dom` — UI библиотека
- `@prisma/client` — ORM клиент
- `typescript` — типизация

### Обработка файлов:
- `pdf-parse` — PDF
- `mammoth` — DOCX
- `xlsx` — Excel
- `jszip` — ZIP/PPTX

### Аутентификация:
- `jsonwebtoken` — JWT токены
- `bcryptjs` — хеширование паролей

### UI:
- `tailwindcss` — стили
- `lucide-react` — иконки
- `react-hook-form` — формы
- `zod` — валидация

---

## 🎓 Тестовые аккаунты

После выполнения `npm run db:seed`:

**Администратор:**
- Email: `admin@kfc.com`
- Пароль: `admin123`

**Сотрудник:**
- Email: `employee@kfc.com`
- Пароль: `user123`

---

## 📈 Статистика проекта

- **Всего файлов для импорта**: 27
- **Поддерживаемые форматы**: PDF, DOCX, PPTX, XLSX, PPT
- **Станций**: 3 (Касса, Кухня, Общие стандарты)
- **Разделов**: ~20
- **Моделей БД**: 10

---

## 🔄 Типичный workflow

1. **Подготовка**: Поместите файлы в `информация/`
2. **Извлечение**: `npm run extract-info`
3. **Интеграция**: `npm run integrate-info`
4. **Организация**: `npm run cleanup-materials && npm run organize-sections`
5. **Проверка**: `npm run show-structure`
6. **Запуск**: `npm run dev`

---

## 🐛 Решение проблем

### Проблема: PDF файлы не обрабатываются
- Проверьте версию `pdf-parse` (используется версия 2.4.5 с классом `PDFParse`)
- Убедитесь, что файлы не повреждены

### Проблема: PPTX файлы показывают только текст
- Это нормально — система извлекает только текст из слайдов
- Для полного отображения используйте PDF версию презентации

### Проблема: Дубликаты материалов
- Запустите `npm run cleanup-materials`
- Проверьте нормализацию названий в скрипте

---

## 📚 Дополнительная информация

- Все пароли хранятся в зашифрованном виде (bcrypt)
- JWT токены действительны 7 дней
- База данных SQLite хранится в `prisma/dev.db`
- Изображения из PDF сохраняются в `public/images/`
- PDF файлы для просмотра копируются в `public/pdfs/`

---

**Версия проекта**: 1.0.0  
**Последнее обновление**: 2024

